# ===========================================
# Mutual Mentor Server Configuration
# ===========================================
# Copy this file to .env and update values

# ===========================================
# ENVIRONMENT
# ===========================================
# Set to 'production' for production deployments
# This enables security hardening features
NODE_ENV=development

# ===========================================
# SERVER
# ===========================================
PORT=3001

# ===========================================
# DATABASE
# ===========================================
# Path to SQLite database file
# For production, use a path outside the application directory
# e.g., /var/lib/mutual-mentor/crm.db
DATABASE_PATH=./data/crm.db

# ===========================================
# AUTHENTICATION (REQUIRED FOR PRODUCTION)
# ===========================================
# IMPORTANT: Generate a strong random secret for production
# You can generate one with: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=CHANGE_THIS_TO_A_STRONG_RANDOM_SECRET_AT_LEAST_64_CHARACTERS

# Token expiration time (default: 24 hours)
JWT_EXPIRATION=24h

# ===========================================
# CORS (REQUIRED FOR PRODUCTION)
# ===========================================
# Frontend URL for CORS configuration
# In production, set this to your actual frontend domain
FRONTEND_URL=http://localhost:5173

# ===========================================
# SECURITY NOTES
# ===========================================
# 1. Never commit the actual .env file to version control
# 2. Use strong, randomly generated secrets in production
# 3. Set NODE_ENV=production for production deployments
# 4. Configure HTTPS at the load balancer/proxy level
# 5. Store the database outside the application directory in production
